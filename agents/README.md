# agents

Agent (subagent) definitions for delegated tasks — e.g. package archaeology sweeps, BTS triage.

Nothing implemented yet — structure and conventions land by PR before anything executable does.
